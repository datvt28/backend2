extends layout

block content
  .auth-container
    img.logo(src='/images/login.png', alt='Đăng nhập')
    h2 🔐 Đăng nhập

    if message
      .alert.alert-danger
        i.fa.fa-exclamation-circle(style='margin-right: 8px')
        | #{message}

    form(method='post', action='/login')
      .form-group
        i.fas.fa-user
        input(type='text', name='username', placeholder='Tên đăng nhập', required)

      .form-group
        i.fas.fa-lock
        input(type='password', name='password', placeholder='Mật khẩu', required)

      button(type='submit') ➡️ Đăng nhập

    .auth-link
      span Chưa có tài khoản?
      a(href='/register') 📝 Đăng ký ngay
